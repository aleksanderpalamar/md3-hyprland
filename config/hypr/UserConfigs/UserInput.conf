# User Input Configuration
# Override input settings here.
# The base input config is in input.conf.

source = ~/.config/hypr/input.conf

# Override keyboard layout (e.g., us, br, de):
# input {
#     kb_layout = us
# }
